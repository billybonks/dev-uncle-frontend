<WorkflowEditor
  @workflow={{this.model}}
  @organisation={{this.repo.organisation}}
  @labels={{this.repo.labels}}
  @transitionAfterSave={{action "transitionAfterSave"}}
  @target={{this.repo}}
/>
