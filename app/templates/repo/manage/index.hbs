{{power-button action=(action "addRule") text="new rule"}}
{{#each repo.rules as |rule|}}
  {{rule-mapper rule=rule labels=repo.labels states=repo.states}}
{{/each}}
{{power-button action=(action "saveRules") text="save"}}
{{outlet}}
