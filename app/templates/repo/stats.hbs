<h1>Pull Requests</h1>
{{#link-to  "repo.manage" repoId}}Settings{{repo.id}}{{/link-to}}
<br />
<br />
<br />
{{#if labels.loadLabels.isIdle}}
  {{#each states.states as |state|}}
    {{state.name}}
    {{pr-stats stateName=state.name pullRequests=model}}
  {{/each}}
{{else}}
  Labels loading
{{/if}}
