{{#if this.repo.labels.length}}
  <Tables-pullrequest-table
    @filters={{this.repo.filters}}
    @repo={{this.repo}}
    @labels={{this.repo.labels}}
    @state={{this.state}}
    @pullRequests={{this.model}}
  />
{{else}}
  Labels loading
{{/if}}
