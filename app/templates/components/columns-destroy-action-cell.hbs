<Button @text="delete" @action={{action @delete @row.rowMeta._rowValue}} />
{{#if @value}}
  <Button @text="deactivate" @action={{action @deactivate @row.rowMeta._rowValue}} />
{{else}}
  <Button @text="active" @action={{action @activate @row.rowMeta._rowValue}} />
{{/if}}
