{{#if this.isLoading}}
  loading
{{else}}
  <PowerSelectMultiple
    @options={{this.users}}
    @selected={{@selection}}
    @palceholder="Select users"
    @onChange={{@onchange}}
    as |user|
  >
    {{user.githubUser}}
  </PowerSelectMultiple>
{{/if}}
